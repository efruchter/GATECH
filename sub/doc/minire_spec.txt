$LETTER [a-zA-Z]
$DIGIT [0-9]
$ASCII [a-zA-Z"\ ]
$NOQUOTE-ASCII [^"] IN $ASCII

$BEGIN begin
$END end
$EQUALS =
$REPLACE replace
$OPEN-PAREN \(
$CLOSE-PAREN \)
$UNION union
$INTERS inters
$PRINT print
$ID $LETTER ($LETTER|$DIGIT|_)*
$ASCII-STRING " ($NOQUOTE-ASCII|\\")* "
$REGEX .*
